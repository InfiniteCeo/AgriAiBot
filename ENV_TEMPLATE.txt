# Copy this content to create your .env file

# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Server Configuration
PORT=3000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-make-it-long-and-random
JWT_EXPIRES_IN=7d

# CORS Configuration (optional, comma-separated list of allowed origins)
# Leave empty or remove for allowing all origins (*)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Instructions:
# 1. Copy this file to .env (not in git)
# 2. Replace the placeholder values with your actual credentials
# 3. Get Gemini API key from: https://aistudio.google.com/apikey
# 4. Get Supabase credentials from your project dashboard
# 5. Run the SQL schema from supabase_setup.sql in your Supabase SQL editor
